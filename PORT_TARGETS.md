# Port targets — `regex-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `regex` that codex-rs uses, and therefore the minimum subset that `regex-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-117] (rank `93`, tier `direct`, status `To Do`)

**Cargo package name**: `regex`
**Rust module name (in `use` statements)**: `regex`

## Summary

- **1** distinct symbols imported by codex-rs
- **3** total `use` statements
- **3** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `regex::Regex`

- **3 `use` site(s)** across **3 codex member crate(s)**: `codex-git-utils`, `codex-secrets`, `codex-shell-command`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `git-utils/src/apply.rs:10` (`codex-git-utils`) — `use regex::Regex`
- `secrets/src/sanitizer.rs:1` (`codex-secrets`) — `use regex::Regex`
- `shell-command/src/command_safety/windows_dangerous_commands.rs:4` (`codex-shell-command`) — `use regex::Regex`
