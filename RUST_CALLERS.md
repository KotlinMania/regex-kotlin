# Rust callers — `regex-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `regex` | `regex` | 24 | 17 | 14 | 3 |

## `regex`

### Inbound symbols (3 distinct, 24 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `regex`.

#### `regex::Regex` — 22 site(s) (3 codex, 19 other, 17 caller crates)

- ✅ `codex-kotlin/codex-git-utils` `src/apply.rs:10` — `use regex::Regex`
- ✅ `codex-kotlin/codex-secrets` `src/sanitizer.rs:1` — `use regex::Regex`
- ✅ `codex-kotlin/codex-shell-command` `src/command_safety/windows_dangerous_commands.rs:4` — `use regex::Regex`
- `insta-kotlin/insta` `src/filters.rs:4` — `use regex::Regex`
- `lalrpop-kotlin/lalrpop` `src/normalize/macro_expand/mod.rs:11` — `use regex::Regex`
- `lalrpop-kotlin/lalrpop` `src/normalize/resolve/test.rs:3` — `use regex::Regex`
- `lalrpop-kotlin/lalrpop` `src/test_util.rs:4` — `use regex::Regex`
- `libwebrtc-kotlin/webrtc-sys-build` `src/lib.rs:27` — `use regex::Regex`
- `libwebrtc-kotlin/yuv-sys` `build.rs:17` — `use regex::Regex`
- `once-cell-kotlin/once_cell` `examples/regex.rs:2` — `use regex::Regex`
- `runfiles-kotlin/cargo-bazel` `src/utils/starlark/label.rs:7` — `use regex::Regex`
- `schemars-kotlin/schemars` `tests/integration/validator.rs:2` — `use regex::Regex`
- _… and 10 more site(s)._

#### `regex::*` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `test-case-kotlin/test-case` `src/lib.rs:98` — `pub_use regex::*`

#### `regex::RegexBuilder` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `starlark-kotlin/starlark` `src/docs/parse.rs:8` — `use regex::RegexBuilder`
